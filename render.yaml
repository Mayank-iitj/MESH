services:
  - type: web
    name: mesh-cron-worker
    env: node
    buildCommand: npm install && npm run prisma:generate
    startCommand: npm run worker
    envVars:
      - key: DATABASE_URL
        sync: false # Set this securely in the Render Dashboard
      - key: STRIPE_SECRET_KEY
        sync: false
